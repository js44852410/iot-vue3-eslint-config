module.exports = {
  rules: {
    // v-model
    'vue/no-v-model-argument': 'off',

    // template for key
    'vue/no-v-for-template-key': 'off',

    // template for key
    'vue/no-multiple-template-root': 'off',
  },
};
